# Rust build artifacts
/target/

# Cargo lock for binaries (keep for libraries)
# Cargo.lock  <-- uncomment if this is a library

# Editor / IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS artifacts
nul
.DS_Store
Thumbs.db

# Claude Code local settings (dev-only, not for release)
.claude/

# Temp files created by Claude Code / editors
tmpclaude-*
*.tmp

# Config files with secrets (do not commit real configs)
/etc/
config.yaml
!examples/config.yaml

# Local Docker Compose environment (may contain credentials)
.env
!.env.example

# Local test databases and state files
*.db
*.sqlite
incident_state.json
